services:
  api:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
    image: community-energy-orchestrator-dev:latest
    user: vscode
    volumes:
      - ..:/workspaces:cached
    command: sleep infinity
    restart: "no"
    healthcheck:
      disable: true

